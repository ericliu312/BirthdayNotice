<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
     >

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="日期列表"
         />
    <ListView 
        android:id="@+id/my_list"
        android:layout_height="0dp"
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:cacheColorHint="#00000000"
        android:divider="@color/expand_list_divider"
        android:dividerHeight="1px"
        android:drawSelectorOnTop="false"
        android:listSelector="@drawable/home_info_bg"
        ></ListView>
    <LinearLayout 
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_vertical|center_horizontal"
        >
    <Button 
        android:id="@+id/add_btn" android:layout_width="fill_parent" android:textSize="16sp"
        android:layout_marginBottom="10dp"
					android:textColor="#ffffffff" android:background="@drawable/login_confirm_btn_bg"
					android:textStyle="bold" android:layout_height="40dp" android:text="添加" />        
    <Button 
        android:id="@+id/register_btn" android:layout_width="fill_parent" android:textSize="16sp"
					android:textColor="#ffffffff" android:background="@drawable/login_confirm_btn_bg"
					android:textStyle="bold" android:layout_height="40dp" android:text="提醒时间" />         
    </LinearLayout>


</LinearLayout>